//! Hardware video decoding through the operating system: Media Foundation on
//! Windows, VideoToolbox on macOS. No external media libraries.
//!
//! Empty until the decoder lands; the network side is useful on its own
//! (`--dump` writes the elementary stream straight to a file).
